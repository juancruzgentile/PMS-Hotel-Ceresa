"""
This module is intentionally broken.

It is used only to test that Ceresa can survive
when one enabled module fails to load.
"""

raise RuntimeError("Intentional broken module for modular loader test.")